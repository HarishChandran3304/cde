# Voice → Open Checkout Spike

Launch the CDE development build with this folder as its workspace. Connect the CDE sidebar, then say:

> Open the file with the checkout logic.

The only exposed Realtime tool opens `src/checkout.ts` and selects `calculateFinalPrice`.
